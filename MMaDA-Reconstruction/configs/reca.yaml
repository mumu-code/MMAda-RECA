run_name: 'null'

wandb:
  entity: null
#   run_id: ${run_name}
  resume: 'auto'

experiment:
    project: mmada-reca
    name: mmada-reca
    output_dir: /data1/MMADA_RECA/outputs/${run_name}
    max_train_examples_t2i: 40000000
    max_train_examples_mmu: 40000000
    save_every: 1000
    eval_every: 1000
    generate_every: 500
    log_every: 10
    log_grad_norm_every: 25
    resume_from_checkpoint: "latest"

model:
    vq_model:
        type: "magvitv2"
        vq_model_name: "/data3/magvitv2"
    mmada:
        pretrained_model_path: "/data3/MMaDA-8B/"
        w_clip_vit: False
        new_vocab_size: 134656
        llm_vocab_size: 126464
        codebook_size: 8192
        num_vq_tokens: 1024
        num_new_special_tokens: 0
        tie_word_embeddings: False

    gradient_checkpointing: True

dataset:
    gen_type: "t2i"
    recon_type: "mid"
    und_type: captioning
    combined_loader_mode: "min_size"
    add_system_prompt: False
    params:
        train_t2i_shards_path_or_url: "/data/dataset/Echo-4o/Instruction-Following-Image/images/*.tar"
        # "/data/dataset/Echo-4o/Instruction-Following-Image/images/*.tar"
        # "/data/dataset/flux-dev-portrait/wds/part{0..9}.tar"
        train_mmu_shards_path_or_url: "" 
        train_lm_shards_path_or_url: ""
        add_caption_prompt: True
        external_caption_path: "/data/dataset/Echo-4o/Instruction-Following-Image/Instruction-Following-Image.jsonl"
        external_journeydb_caption_path: ""
        external_laion12m_caption_path: ""
        external_cc12m_caption_path: ""
        external_vqa_caption_path: "/data2/LLaVA-Instruct-150K/llava_v1_5_mix665k.json"
        validation_prompts_file: "eval/validation_prompts/mix-sft.txt"
        mmu_image_root: /home/xl/project/MaskGRPO/generated_samples/simple/tiger
        vqa_images_path: "/data2/LLaVA-Instruct-150K/tuning_data"
        shuffle_buffer_size: 100
        num_workers: 8
        resolution: 512
        pin_memory: True
        persistent_workers: True


    preprocessing:
        max_seq_length: 256 # for text tokens
        resolution: 512
        center_crop: False
        random_flip: False

optimizer:
    name: adamw
    params: # default adamw params
        learning_rate: 3e-6
        scale_lr: False # scale learning rate by total batch size
        beta1: 0.9
        beta2: 0.999
        weight_decay: 1e-3
        epsilon: 1e-8

lr_scheduler:
    scheduler: "constant_with_warmup"
    params:
        learning_rate: ${optimizer.params.learning_rate}
        warmup_steps: 50
        min_lr_scale: 0.5

training:
    gradient_accumulation_steps: 2
    noise_type: "mask"
    batch_size_t2i: 1
    batch_size_lm: -1
    batch_size_mmu: 1
    batch_size_recon: 2
    mixed_precision: "bf16"
    enable_tf32: True
    seed: 24601
    max_train_steps: 1000000
    overfit_one_batch: False
    cond_dropout_prob: 0.1
    min_masking_rate: 0.3
    label_smoothing: 0.0
    max_grad_norm: 1
    guidance_scale: 4.0
    generation_timesteps: 18
    t2i_coeff: 1.0
    lm_coeff: 0.1
    mmu_coeff: 1.0
    recon_coeff: 1.0
    down_scale: 2
    resize_back: False